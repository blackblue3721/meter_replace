# 越疆 CR5 电表自动更换项目组会汇报

汇报日期：2026-08-14  
本次主题：从 MoveIt/RViz 仿真闭环到真实 CR5 同步执行闭环

## 1. 本次汇报结论

本阶段完成了项目的一次关键跨越：不再只有 mock 控制器中的仿真运动，而是建立了真实 CR5 反馈、RViz 数字场景、MoveIt 轨迹和 Python/TCP-IP 执行之间的闭环。

已经验证：

- 真实 CR5 六轴角度持续发布到 ROS 2 `/joint_states`；
- RViz 中的 CR5 与实机同步运动，现场观察无明显延迟；
- MoveIt 生成并审核的轨迹可以转换为越疆控制命令；
- 通过分段调试、连续 mock 回归、哈希校验和起点对齐，真实 CR5 完成了一次桌面到指定表位再返回 Home 的完整空载路径；
- 执行记录 `install-20260807-01` 的八个阶段均为完成状态。

当前边界：实体夹爪尚未安装，因此夹紧和释放仍是虚拟握手。本阶段证明的是“规划—审核—下发—反馈—验证”的机械臂闭环，而不是最终真实夹持工艺。

## 2. 上一阶段核心成果留档

上一阶段已完成并继续沿用的基础包括：

- CR5 的 URDF/Xacro、MoveIt SRDF、控制器和 RViz 配置；
- 按实测尺寸建立的工作台、电表箱、四个表位和碰撞场景；
- 电表 CAD 可视模型与 `160 × 110 × 70 mm` 简化碰撞体；
- 桌面电表安装状态机和表位电表拆取状态机；
- OMPL 自由空间规划、Pilz LIN 直线下降/插入/抽出；
- MuJoCo 重力、摩擦、接触和占位夹爪保持实验。

这些内容本次不再逐项展开，作为当前实机迁移工作的基础保留。

## 3. 学习成果：本阶段学到了什么

### 3.1 理解了“RViz 同步”不是简单播放动画

真实链路为：

```text
CR5 30004 实时反馈
        ↓
只读 Python joint-state publisher
        ↓
ROS 2 /joint_states
        ↓
robot_state_publisher + TF
        ↓
RViz 真实姿态显示
```

RViz 中的机器人不是预设动画，而是由真实 CR5 的六轴反馈驱动。DobotStudio、TCP 反馈和 RViz 三者姿态一致后，才能确认关节顺序、正负号和单位映射正确。

### 3.2 理解了规划器、轨迹和真实控制器的职责边界

MoveIt 负责：

- 逆运动学；
- 碰撞检查；
- OMPL/Pilz 轨迹规划；
- 输出带时间和关节位置的轨迹点。

真实 CR5 控制器负责：

- 接收 `JointMovJ` 或周期性 `ServoJ`；
- 执行内部伺服和安全限制；
- 返回真实关节、模式和报警状态。

因此不能把“MoveIt 规划成功”直接等同于“真机一定能执行”。中间必须有轨迹转换、范围检查、起点连续性和反馈确认。

### 3.3 学会了区分 JointMovJ 与 ServoJ

- `JointMovJ`：适合已经审核的关节空间目标，例如预抓取和电表语义翻转；
- `ServoJ`：适合保留 MoveIt 多点轨迹形状，例如直线下降、抬升、箱前转移、插入和撤离；
- 本项目验证了约 `30 ms` 的 ServoJ 周期，避免把多点轨迹错误压缩成一个终点动作；
- 对每段动作都检查实际终点误差，不依赖固定 `sleep` 判断完成。

### 3.4 学会了轨迹完整性和连续性审核

连续轨迹包不是一组可以随意替换的 JSON：

- 每段轨迹保存 SHA256；
- 后一段起点必须等于前一段终点；
- 真机实时起点必须与第一段起点一致；
- 单关节变化、单点步长和 J5 翻转范围都有保护阈值；
- 每次完整执行使用唯一 `run-id` 和独立回执。

这使“运行了什么轨迹”可以追溯，而不是只凭现场视频判断。

### 3.5 学会了真实机器人调试的安全递进方法

本次采用的顺序是：

```text
只读网络与状态检查
→ J6 小角度测试
→ J1 往返测试
→ 单段 MoveIt 轨迹
→ 预抓取终点
→ 各工艺段逐段验证
→ 连续 mock 回归
→ 完整真机 dry-run
→ 唯一 run-id 的整流程执行
```

一次真实碰撞和一次 ServoJ 节拍问题也说明：仿真模型未包含的临时障碍物、控制器队列状态和通信周期都可能成为真机风险。失败后不能盲目 `continue()` 或重复发送命令。

## 4. 工作成果：本阶段实际做到了什么

### 4.1 打通真实 CR5 TCP/IP 通信

已验证：

```text
CR5 IP：192.168.5.1
29999：Dashboard 状态与参数
30003：运动命令
30004：实时反馈
```

完成 RobotMode、GetAngle、GetPose、GetErrorID 和实时反馈交叉检查，并验证 Linux 虚拟机到 CR5 的网络链路。

### 4.2 建立只读真实关节发布器

入口：

```text
robot/real/cr5_tcp/publish_cr5_joint_states.py
```

该节点只读取 30004，不连接运动端口，将厂商 J1—J6 反馈转换为 ROS 2 `JointState`。关节映射已通过 DobotStudio 与 RViz 实物姿态对照验证。

### 4.3 建立真实反馈专用 MoveIt/RViz 场景

入口：

```text
ros2 launch meter_grasp real_arm_plan_only_scene.launch.py
```

这个场景加载机器人、TF、MoveIt、RViz、电表箱、电表 CAD 和表位，但不启动 mock 控制器，因此 RViz 显示来自真实反馈，不会误把仿真执行当成真机执行。

### 4.4 完成 MoveIt 轨迹到 CR5 的执行接口

已实现：

- ROS 关节弧度与厂商关节角度转换；
- `JointMovJ` 目标执行；
- 30 ms 周期的 `ServoJ` 多点轨迹执行；
- 运动前 mode、报警、静止状态和起点检查；
- 运动后实时反馈误差检查；
- 失败停止、运行回执和禁止重复运行。

### 4.5 完成连续八阶段轨迹包

连续 mock 场景从真实调试 Home 出发，按真实状态顺序生成：

```text
pregrasp
→ grasp_candidate
→ lift
→ semantic_flip
→ slot_transfer
→ slot_insert
→ slot_retreat
→ return_home
```

轨迹包通过：

- 连续 mock 执行；
- RViz 人工方向审核；
- 八段 SHA256 校验；
- 相邻阶段边界检查；
- 关节变化与语义翻转保护检查。

### 4.6 完成真实 CR5 完整空载安装路径

统一执行入口：

```text
robot/real/cr5_tcp/run_real_meter_install.py
```

运行编号：

```text
install-20260807-01
```

正式回执：

```text
logs/commissioning/runs/install-20260807-01/meter_install.json
```

回执记录：

- `status = complete`；
- 八个阶段全部完成；
- 每段轨迹摘要完整；
- 真实 CR5 最终返回经审核的 Home 分支。

### 4.7 形成可复现和可审计的工程流程

- `review`：按终端拆分的仿真、同步、检查和执行顺序；
- `config/hardware.yaml`：IP、端口、速度、阈值和单次授权；
- `config/real_cr5_install_manifest.json`：完整轨迹清单；
- `logs/real_cr5/full_install_bundle/`：八段已审核轨迹；
- `logs/commissioning/`：分段与整流程真实执行证据。

## 5. 本阶段结果的意义

此前完成的是“算法在仿真中可行”；本阶段证明的是：

```text
真实反馈能进入 ROS 2
→ RViz 能显示真实姿态
→ MoveIt 轨迹能经过安全审核
→ Python 能控制真实 CR5 执行
→ 执行结果能回到 ROS 2 并被验证
```

这说明当前软件已经具备从数字规划走到真实机器人执行并回读验证的基础能力。

## 6. 当前不足与边界

- 尚无实体夹爪，未验证真实夹持力、TCP 和负载；
- 电表箱和表位仍以人工测量的简化几何为主；
- 电表仍使用简化碰撞体，本次没有验证真实接触和装配精度；
- 当前完成的是右侧单臂，双臂区域锁和协同尚未开始实机验证。

## 7. 本次组会总结

本阶段的主要学习收获，是理解并实际验证了 MoveIt 规划轨迹与工业机器人控制器之间还需要关节映射、轨迹转换、通信节拍、起点对齐、反馈检查和安全授权。

本阶段的主要工作成果，是让同一台真实 CR5 同时进入 ROS 2/RViz 数字场景，并完成一次可追溯的八阶段连续空载运行。

可以用以下两句话区分：

> 学到了什么：掌握了机器人从数字规划到真机执行的完整技术链和安全调试方法。  
> 做到了什么：真实 CR5 与 RViz 同步，MoveIt 轨迹成功下发，八阶段空载流程执行完成并留下正式回执。
