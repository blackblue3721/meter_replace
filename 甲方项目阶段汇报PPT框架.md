# 越疆 CR5 自动更换电表项目阶段汇报 PPT

汇报日期：2026-08-14  
建议时长：15 分钟  
正文：12 页  
本次主题：数字仿真与真实机械臂同步执行闭环

## 第 1 页：封面

### 标题

# 越疆 CR5 自动更换电表项目阶段成果汇报

## 从数字仿真走向真实机械臂闭环执行

技术关键词：`ROS 2 · MoveIt 2 · RViz 2 · MuJoCo · CR5 TCP/IP`

### 页面内容

- 左侧：实验室真实 CR5 与电表箱照片；
- 右侧：同一姿态下的 RViz 数字场景截图；
- 中间箭头：`真实设备 ↔ 数字系统`。

### 汇报词

> 上一阶段我们完成了机器人、工作台、电表箱和电表的数字化建模，以及单臂安装和拆取动作的仿真验证。本阶段进一步打通了真实 CR5：真实机械臂运动时，RViz 中的数字机器人能够同步显示；计算机规划的完整路径也已经由真实 CR5 连续执行完成。

## 第 2 页：本阶段一句话成果

### 标题

# 已打通“规划—真机执行—实时反馈—结果验证”闭环

### 页面正文

```text
MoveIt 规划安全路径
        ↓
Python 审核并转换轨迹
        ↓
真实 CR5 执行动作
        ↓
CR5 实时反馈六轴状态
        ↓
RViz 同步显示并验证结果
```

页面底部结论：

> 项目已经从“电脑里能够运行”推进到“真实机械臂能够按照审核后的数字轨迹完成整套空载动作”。

### 汇报词

> 这次最重要的进展不是又增加了一个动画，而是形成了闭环。规划结果不再停留在 RViz 中，而是能够发送给真实 CR5；机器人实际到达的关节状态又会反馈到 ROS 2 和 RViz 中进行核验。

## 第 3 页：上一阶段基础保留

### 标题

# 已有数字化基础继续支撑本阶段实机迁移

### 页面正文

- 按真实尺寸建立工作台、电表箱、四个表位和右侧 CR5；
- 建立 CR5、占位夹爪和电表 CAD 模型；
- 使用 `MoveIt 2 + OMPL + Pilz LIN` 完成避碰、直线接近和插入规划；
- 使用 Python 状态机完成仿真安装与拆取；
- 使用 MuJoCo 验证重力、摩擦和占位夹爪接触。

页面结论：

> 本阶段没有推倒原有成果，而是在原有软件架构上增加真实机械臂接口。

### 汇报词

> 之前完成的模型、碰撞场景、路径规划和状态机都得到了保留。我们新增的是一条真实机器人执行链，没有为了连接真机而推倒重做原有系统。

## 第 4 页：真实 CR5 接入架构

### 标题

# ROS 2 与越疆 CR5 TCP/IP 已完成连接

### 页面正文

| 通道 | 作用 | 当前状态 |
|---|---|---|
| `29999` | 读取模式、关节、位姿和报警 | 已验证 |
| `30003` | 发送审核后的运动指令 | 已验证 |
| `30004` | 获取真实六轴实时反馈 | 已验证 |

软件分工：

| 软件 | 项目中的作用 |
|---|---|
| `ROS 2` | 连接机器人状态、坐标、场景和任务 |
| `MoveIt 2` | 逆运动学、碰撞检测和轨迹规划 |
| `RViz 2` | 同步显示真机姿态和规划路径 |
| `Python` | 安全检查、轨迹审核、任务执行和日志 |
| `DobotStudio Pro` | 真机模式、使能、报警和现场维护 |

### 汇报词

> 我们没有绕过越疆控制器，而是使用其 TCP/IP 二次开发接口。ROS 2 和 MoveIt 负责计算，Python 负责安全审核和任务编排，CR5 控制器负责真实伺服执行。

## 第 5 页：RViz 与真实 CR5 同步

### 标题

# 真实机械臂转动，数字机器人同步转动

### 页面正文

```text
CR5 实际关节反馈
→ ROS 2 /joint_states
→ TF 机器人坐标
→ RViz 数字姿态
```

现场验证结果：

- DobotStudio 显示的 J1—J6 与 ROS 反馈一致；
- RViz 与实机运动方向一致；
- 现场观察无明显同步延迟；
- 只读同步程序与运动程序相互隔离。

### 页面素材

- 左侧插入真机动作照片；
- 右侧插入相同姿态的 RViz 截图；
- 两图标注同一组 J1—J6。

### 汇报词

> RViz 中显示的不是预先录制的动画，而是来自真实 CR5 的实时反馈。真机状态、数字模型和规划结果因此能够在同一个界面中对照检查。

## 第 6 页：从路径规划到真机动作

### 标题

# MoveIt 轨迹已经能够安全转换为真实 CR5 动作

### 页面正文

两类执行方式：

- `JointMovJ`：执行预抓取和姿态翻转等关节目标；
- `ServoJ`：按约 30 ms 周期跟随 MoveIt 多点轨迹，保留下降、抬升、插入和撤离的路径形状。

每段轨迹执行前检查：

- 机器人已使能、静止、无报警；
- 真机当前角度与轨迹起点一致；
- 轨迹没有超出单关节保护范围；
- 轨迹文件 SHA256 未被修改。

每段轨迹执行后检查：

- 读取真实六轴反馈；
- 计算目标误差；
- 只有误差合格才进入下一阶段。

### 汇报词

> 真实机器人不是简单接收一个终点。对于关键直线动作，我们保留 MoveIt 生成的多点轨迹；同时对起点、范围、文件完整性和终点反馈逐项检查。

## 第 7 页：安全调试过程

### 标题

# 从 2° 小动作逐步扩大到完整流程

### 页面正文

```text
只读网络检查
→ J6 小角度测试
→ J1 往返测试
→ 单段轨迹测试
→ 八个阶段逐段验证
→ 连续 mock 回归
→ 完整真机 dry-run
→ 单次授权整流程执行
```

已建立的安全机制：

- 默认 dry-run，不发送运动；
- 现场急停优先；
- 禁止未知状态下自动 `continue()`；
- 每次完整运行使用唯一编号；
- 失败立即停止并保留执行记录；
- 临时障碍物必须进入现场检查和碰撞模型。

### 汇报词

> 我们没有直接让机械臂跑完整动作，而是从只读、小角度、单段到整段逐级扩大。这既减少设备风险，也让每个问题都能定位到具体阶段。

## 第 8 页：完整空载安装路径已跑通

### 标题

# 真实 CR5 已连续完成八阶段安装路径

### 页面正文

```text
预抓取
→ 下降到抓取位
→ 抬升
→ 调整电表安装方向
→ 转移到目标表位前方
→ 直线插入
→ 直线退出
→ 返回 Home
```

执行结果：

| 项目 | 结果 |
|---|---|
| 运行编号 | `install-20260807-01` |
| 完成阶段 | `8 / 8` |
| 运行状态 | `complete` |
| RViz 同步 | 正常 |
| 最终返回 Home | 成功 |

醒目标注：

> 当前无实体夹爪，本次为占位工具的空载路径闭环。

### 汇报词

> 真实 CR5 已经连续完成从桌面预抓取、抬升、转向、电表箱前转移、插入、退出到返回 Home 的完整路径。夹爪到货后，我们将在相同软件链上补充真实夹持和工具信号。

## 第 9 页：成果可复现、可追溯

### 标题

# 不只“现场跑通”，还保存了完整工程证据

### 页面正文

- 八段轨迹均保存独立 SHA256；
- 相邻动作的起点和终点连续；
- 真实运行使用唯一 `run-id`；
- 每个完成阶段写入执行回执；
- 项目参数集中保存 IP、速度、阈值、表位和坐标；
- `review` 文档保存每个终端的启动顺序和复现命令。

主要证据：

```text
logs/real_cr5/full_install_bundle/
logs/commissioning/runs/install-20260807-01/meter_install.json
```

### 汇报词

> 项目结果不是依赖某一次人工操作。运行了哪条轨迹、完成了哪些阶段、真实终点是多少，都保存在工程记录中，方便后续复现和问题追踪。

## 第 10 页：当前能力边界

### 标题

# 本次成果聚焦机械臂运动闭环，不扩大成果口径

### 页面正文

已经完成：

- 真实 CR5 状态进入 ROS 2；
- RViz 与真机同步；
- MoveIt 路径可下发到真机；
- 八阶段完整空载路径连续执行；
- 运行过程可审核、可停止、可追溯。

本次尚未声称完成：

- 真实夹爪夹持电表；
- 带负载的真实搬运；
- 真实电表插装精度验证；
- 双机械臂协同作业。

### 汇报词

> 我们对本阶段成果保持清晰边界：已经跑通的是机械臂数字规划与真实执行闭环。由于实体夹爪尚未安装，本次不把空载路径验证表述成真实电表安装完成。

## 第 11 页：本阶段形成的项目资产

### 标题

# 软件、模型、轨迹和运行记录均已沉淀

### 页面正文

| 项目资产 | 已形成内容 |
|---|---|
| 数字模型 | CR5、工作台、电表箱、四表位、电表 CAD |
| 规划能力 | OMPL 避碰、Pilz LIN 直线工艺段 |
| 仿真能力 | RViz/MoveIt 运动验证、MuJoCo 接触验证 |
| 真机接口 | CR5 状态读取、关节同步、轨迹执行 |
| 安全机制 | dry-run、范围检查、起点对齐、唯一授权 |
| 工程记录 | 八段轨迹摘要、完整运行回执、复现手册 |

### 汇报词

> 本阶段留下的不只是一次演示。模型、配置、轨迹、执行程序和运行证据都已经进入项目目录，可以继续复用和审计。

## 第 12 页：阶段总结

### 标题

# 从“仿真可行”推进到“真机闭环可行”

### 页面正文

```text
真实工作区数字化
        ↓
MoveIt 路径规划与碰撞检查
        ↓
连续轨迹审核与完整性校验
        ↓
真实 CR5 执行
        ↓
RViz 同步与反馈验收
```

最终结论：

> 项目已经建立真实 CR5 的数字化规划、同步显示、受控执行和结果追溯闭环，为后续工程工作提供了稳定的软件与机器人执行基础。

### 汇报词

> 本阶段完成了从仿真到真机的关键迁移。真实 CR5 能够执行 MoveIt 审核轨迹，RViz 能够同步显示真实姿态，整个过程有安全门和运行记录。这是本次阶段汇报的核心成果。
